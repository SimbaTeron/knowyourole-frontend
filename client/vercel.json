{
  "framework": "nextjs",
  "buildCommand": "npm install --include=dev && npm run build",
  "installCommand": "npm install --include=dev",
  "devCommand": "npm run dev",
  "crons": [
    {
      "path": "/api/internal/privacy/purge",
      "schedule": "17 3 * * *"
    }
  ]
}

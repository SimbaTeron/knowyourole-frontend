Build Phase 2 for KnowRole in Replit: Question Bank & Quiz Core, extending Phase 1 App.jsx (import states: step, tier, mood, location, theme, funMode). Post-theme toggle (step=2), route to <Quiz /> on "Begin Journey" (useNavigate or state.step++). Polish for professional fun: Framer Motion fade-ins (0.2s y:20→0 on cards), haptics (navigator.vibrate(50) on swipes/selects), A11y (aria-label="Swipe left for [option1], right for [option2]"), dynamic length (start 15q free, if engagement>80% auto-add 5 "path extend" via score calc), progress teasers ("3 swipes to your spark!"), pause/save every 5q (localStorage session). Free 20q tease; paid modal ($5 Stripe unlock loads 40q).

Question Bank (/data/questions.json): 40 validated binaries (20 free + 20 paid, 2 versions/tier for retests)—balance 30% MBTI (E/I,S/N,T/F,J/P), 30% DISC (D/I/S/C), 40% Big Five (O/C/E/A/N) from IPIP/public sources; 20% wildcards (neutral/cultural proxies e.g. "Market haggle or quiet buy?"). Tier-scale: 7-12 (10q v1/v2, 4s, emoji swipes e.g. 🐻 vs 🏗️); 13-18 (20q, 5s, teen scenarios); 19-25 (25q, 6s, real-world); 25+ (30q, 7s, hypotheticals). Example seed:
[
  {"id":1, "prompt": "Group game: Lead cheer or plan trick?", "options": ["Cheer (E)", "Plan (I)"], "psych": "MBTI-EI", "time":4, "tier":"7-12", "version":"v1", "validated":true, "wildcard":false},
  {"id":2, "prompt": "Deadline: Jump in or outline first?", "options": ["Jump (D)", "Outline (C)"], "psych": "DISC-DC", "time":6, "tier":"19-25", "version":"v1", "validated":true, "wildcard":false},
  // Full 40: Even psych distro, randomize order, wildcards like {"prompt": "Festival: Dance crowd or chat corner?", "options": ["Dance", "Chat"], "psych": "Big Five-E", "wildcard":true}
]

Quiz.jsx: Full-screen swipe core (react-tinder-card for binaries: onSwipe=(dir) => {score[dir==='left'?0:1]; vibrate(50); nextQ();}, fallback tap if no touch). Timer: Visual arc (div w/ width 100%→0% bg-red-500 transition, setTimeout( time*1000 ) buzzer Howler 'ding' or console.beep). Batch 3-4 rounds (progress bar path-milestones: theme-adapt e.g. Light crisp grids, Dark shadowy, Random proc HSL for cards from 7 sub-themes: sunburst (radial orange), neon-urban (zigzag cyan), forest-whisper (leafy green), ocean-drift (wave blue), desert-bloom (spiky red), city-pulse (grid gray), meadow-dream (loop pastel)—random select on load, log anon 'Random: sunburst - Openness +1'). Pause button every 5q (save to localStorage, resume on return). Teasers: Mid-round motion.span "2 swipes to spark!" (fade 0.3s). Theme proxy: Light=Conscientious nudge (e.g., +5% C score), Dark=Neurotic/Intro (subtle -E), Random=Open/Extravert (wildcard q bonus).

Server (/server/score.js): POST /score—tally (e.g., EI = leftE_count / total *100 >50?'E':'I'; blend 'INTP-D' w/ Big Five %s), weight wildcards 0.8, return {mbti, disc, bigfive:{O:80,...}, proxyNudge: theme==='Random'?'Openness boost':''}. SQLite temp anon sessions for paid arcs/retests.

Integrate: Quiz complete → Results tease (e.g., "Your path: INTP trailblazer—unlock sparks?"). Output: Quiz.jsx (full w/ states: qIndex, scores, engagement), questions.json (40q), score.js, updated App.jsx routing. Test: 15q dynamic flow (add extras if engaged), swipe scores blend, Random sub-theme burst, pause/resume.